version: "3.9"

services:
  scanner:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/backend/data   # persist SQLite database
    env_file:
      - backend/.env
    restart: unless-stopped
